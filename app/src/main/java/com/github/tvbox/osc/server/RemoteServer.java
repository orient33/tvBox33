package com.github.tvbox.osc.server;

/**
 * Stub for Guard spider's ProxyOrigin port discovery.
 * The spider reads {@link #serverPort} via reflection or direct class reference
 * to find the local proxy server.
 */
public class RemoteServer {
    public static int serverPort = 8964;
}
